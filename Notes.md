What's Ts
TypeScript is a programming language developed by microsoft to address the shortcomming's of Js.
Type script is a programming language built on top of Js, so every Js file is a valid Ts file.

Ts feature's
It offer's static typing
Ts is -> Js with type checking
It check's the type at the compile time only , so that we dont have to run our complete application to figure out the problem's.
It provide's smoothles code completion due to it's compatibility with code editor's.
It provide's refactoring
Shorthand notation's

Drawback's
Ts involve's a compilation step because browser's don't understand Ts code, So we give our code to Ts compiler which complie's our code into Js -> This process is called Transpilation.
It's a bit lengtheir than vanilla Js or Js.
